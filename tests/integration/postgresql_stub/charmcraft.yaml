name: postgres-stub
type: charm
bases:
  - build-on:
    - name: ubuntu
      channel: "24.04"
    run-on:
    - name: ubuntu
      channel: "24.04"
summary: A stub charm to mock postgres relations.
description: |
  A lightweight charm that pretends to be Postgres 
  to test relation hooks in other charms.
peers:
  peers:
    interface: stub-peers
provides:
  db: 
    interface: postgresql
